# QUICK DEPLOY

快速部署内容已合并到 `README.md`，请直接查看：

- `README.md` 的“快速部署到 Vercel”
- `README.md` 的“部署后验证”
- `README.md` 的“环境变量（Vercel）”

如果你只需要最短路径：

1. 点击 README 中的 Vercel 一键部署按钮。
2. 按提示设置环境变量并完成部署。
3. 用 README 中的 `curl` 命令验证 `/health` 与 `/api/search`。
